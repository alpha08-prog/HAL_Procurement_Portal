import ScreenStub from '../../components/ScreenStub.jsx';

export default function PaymentRegister() {
  return (
    <ScreenStub
      title="Payment Record & History Register"
      note="Read-only audit log with filters, export and cycle-time metrics. Coming next."
    />
  );
}
