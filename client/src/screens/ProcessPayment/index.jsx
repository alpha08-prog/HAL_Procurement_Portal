import ScreenStub from '../../components/ScreenStub.jsx';

export default function ProcessPayment() {
  return (
    <ScreenStub
      title="Process Payment"
      note="Payment desk — forward to CPPC (PPR no/date) or send back with remark. Coming next."
    />
  );
}
